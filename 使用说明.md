# 🚀 ClaudeAPI 多用户 SaaS 系统使用指南

> 一个功能完整的 Claude API 代理服务，支持多用户、计费管理、账号池管理

---

## 📖 目录

- [快速开始](#快速开始)
- [系统架构](#系统架构)
- [配置说明](#配置说明)
- [使用示例](#使用示例)
- [管理员指南](#管理员指南)
- [用户指南](#用户指南)
- [API 文档](#api-文档)
- [常见问题](#常见问题)

---

## 🎯 快速开始

### 1. 启动服务器

```bash
# 默认端口 9091
PORT=9091 node src/index-new.js

# 或在后台运行
PORT=9091 node src/index-new.js > /tmp/server.log 2>&1 &
```

### 2. 访问管理后台

打开浏览器访问：**http://localhost:9091**

- **管理员密钥**：`zxc123`（首次登录后请立即修改）

### 3. 创建用户

1. 登录管理后台
2. 点击"用户管理"标签
3. 点击"创建用户"按钮
4. 填写用户信息并设置初始余额
5. 复制生成的 API Key 发给用户

---

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    ClaudeAPI 系统                        │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │  登录页面     │  │  用户中心     │  │  管理后台     │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
│                                                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │              认证 & 计费中间件                     │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │  用户管理     │  │  计费系统     │  │  账号池       │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
│                                                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │              SQLite 数据库                         │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

## ⚙️ 配置说明

### 环境变量

```bash
PORT=9091                    # 服务器端口
DB_PATH=./data/database.db   # 数据库路径
ADMIN_PATH=/admin.html       # 管理页面路径（可自定义隐藏）
```

### Claude Code 配置

在项目根目录创建或编辑 `.claude.json`：

```json
{
  "env": {
    "ANTHROPIC_BASE_URL": "http://localhost:9091",
    "ANTHROPIC_AUTH_TOKEN": "your-api-key-here"
  }
}
```

**说明**：
- `ANTHROPIC_BASE_URL`：替换为您的服务器地址
- `ANTHROPIC_AUTH_TOKEN`：替换为您的 API Key

---

## 💡 使用示例

### Claude Code

配置好 `.claude.json` 后，直接使用 Claude Code 即可：

```bash
claude "Hello, Claude!"
```

### cURL

```bash
curl -X POST http://localhost:9091/v1/messages \
  -H "Content-Type: application/json" \
  -H "x-api-key: your-api-key-here" \
  -d '{
    "model": "claude-sonnet-4-5-20250929",
    "max_tokens": 1024,
    "messages": [
      {
        "role": "user",
        "content": "Hello, Claude!"
      }
    ]
  }'
```

### Python

```python
import anthropic

# 方式一：使用环境变量
import os
os.environ['ANTHROPIC_BASE_URL'] = 'http://localhost:9091'
os.environ['ANTHROPIC_API_KEY'] = 'your-api-key-here'

client = anthropic.Anthropic()

# 方式二：直接配置
client = anthropic.Anthropic(
    api_key="your-api-key-here",
    base_url="http://localhost:9091/v1"
)

# 发送请求
response = client.messages.create(
    model="claude-sonnet-4-5-20250929",
    max_tokens=1024,
    messages=[
        {"role": "user", "content": "Hello, Claude!"}
    ]
)

print(response.content[0].text)
```

### Node.js

```javascript
import Anthropic from '@anthropic-ai/sdk';

const client = new Anthropic({
  apiKey: 'your-api-key-here',
  baseURL: 'http://localhost:9091/v1'
});

const response = await client.messages.create({
  model: 'claude-sonnet-4-5-20250929',
  max_tokens: 1024,
  messages: [
    { role: 'user', content: 'Hello, Claude!' }
  ]
});

console.log(response.content[0].text);
```

### 流式响应

```python
import anthropic

client = anthropic.Anthropic(
    api_key="your-api-key-here",
    base_url="http://localhost:9091/v1"
)

with client.messages.stream(
    model="claude-sonnet-4-5-20250929",
    max_tokens=1024,
    messages=[{"role": "user", "content": "写一首诗"}]
) as stream:
    for text in stream.text_stream:
        print(text, end="", flush=True)
```

---

## 👨‍💼 管理员指南

### 登录管理后台

1. 访问 http://localhost:9091
2. 输入管理密钥：`zxc123`
3. 进入管理员控制台

### 用户管理

#### 创建用户

```
用户管理 → 创建用户
```

填写信息：
- **用户名**：用户的唯一标识
- **初始余额**：用户的起始余额（USD）
- **输入价格**：输入 Token 价格（$/MTok，默认 3.0）
- **输出价格**：输出 Token 价格（$/MTok，默认 15.0）

系统会自动生成 API Key，格式：`sk-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx`

#### 用户充值

```
用户管理 → 找到用户 → 充值
```

填写充值金额和备注，系统会记录充值历史。

#### 删除用户

```
用户管理 → 找到用户 → 删除
```

⚠️ **警告**：删除用户不可恢复，请谨慎操作！

### Kiro 账号池管理

```
Kiro账号池 → 查看/管理账号
```

功能：
- 查看所有 Kiro 账号状态
- 刷新单个账号额度
- 刷新全部账号额度
- 添加/删除账号
- 启用/禁用账号

### 系统设置

```
系统设置 → 修改管理密钥
```

⚠️ **重要**：修改管理密钥后需要重新登录！

### 查看统计

管理后台顶部显示实时统计：
- **活跃账号**：当前可用的 Kiro 账号数
- **总请求**：累计 API 调用次数
- **输入/输出 Tokens**：累计 Token 使用量
- **运行时间**：服务器运行时长

---

## 👤 用户指南

### 登录用户中心

1. 访问 http://localhost:9091
2. 输入您的 API Key
3. 进入用户中心

### 查看余额

用户中心顶部显示当前余额，余额不足时无法调用 API。

### 使用 API

#### 获取配置信息

在用户中心可以看到：
- **API Key**：您的访问凭证（可复制）
- **Base URL**：API 端点地址（可复制）

#### 配置您的应用

将上述信息配置到您的应用中即可使用。

### 查看使用统计

```
使用统计 → 查看图表和数据
```

包括：
- 最近 7 天请求趋势
- 最近 7 天消费趋势
- 按模型统计的使用情况

### 查看请求日志

```
请求日志 → 查看最近 100 条记录
```

每条记录包含：
- 时间
- 模型
- 输入/输出 Token 数
- 费用
- 状态（成功/失败）

### 查看充值记录

```
充值记录 → 查看历史充值
```

---

## 📚 API 文档

### 认证

所有 API 请求需要在 Header 中包含 API Key：

```
x-api-key: your-api-key-here
```

或使用 Authorization Header：

```
Authorization: Bearer your-api-key-here
```

### 可用模型

```bash
GET /v1/models
```

**响应示例**：

```json
{
  "object": "list",
  "data": [
    {
      "id": "claude-sonnet-4-5-20250929",
      "object": "model",
      "display_name": "Claude Sonnet 4.5",
      "max_tokens": 32000
    },
    {
      "id": "claude-opus-4-5-20251101",
      "object": "model",
      "display_name": "Claude Opus 4.5",
      "max_tokens": 32000
    },
    {
      "id": "claude-haiku-4-5-20251001",
      "object": "model",
      "display_name": "Claude Haiku 4.5",
      "max_tokens": 32000
    }
  ]
}
```

### 发送消息

```bash
POST /v1/messages
```

**请求体**：

```json
{
  "model": "claude-sonnet-4-5-20250929",
  "max_tokens": 1024,
  "messages": [
    {
      "role": "user",
      "content": "Hello, Claude!"
    }
  ],
  "stream": false
}
```

**响应示例**：

```json
{
  "id": "msg_xxx",
  "type": "message",
  "role": "assistant",
  "content": [
    {
      "type": "text",
      "text": "Hello! How can I help you today?"
    }
  ],
  "model": "claude-sonnet-4-5-20250929",
  "usage": {
    "input_tokens": 10,
    "output_tokens": 15
  }
}
```

### 计费说明

**费用计算公式**：

```
总费用 = (输入 Tokens / 1,000,000) × 输入价格 + (输出 Tokens / 1,000,000) × 输出价格
```

**默认价格**：
- 输入：$3.0 / MTok
- 输出：$15.0 / MTok

**余额检查**：
- 请求前会预估最大费用（假设输出 32K tokens）
- 余额不足时返回 `402 Payment Required`
- 请求成功后自动扣费

### 错误码

| 状态码 | 说明 |
|--------|------|
| 200 | 请求成功 |
| 401 | 认证失败（API Key 无效） |
| 402 | 余额不足 |
| 400 | 请求参数错误 |
| 500 | 服务器内部错误 |

---

## ❓ 常见问题

### Q: 如何修改管理密钥？

**A**: 登录管理后台 → 系统设置 → 修改管理密钥

### Q: 用户余额不足怎么办？

**A**: 联系管理员充值。管理员可以在"用户管理"中为用户充值。

### Q: 如何查看 API 使用情况？

**A**:
- **用户**：登录用户中心查看个人统计
- **管理员**：登录管理后台查看全局统计

### Q: 支持哪些模型？

**A**: 目前支持：
- Claude Sonnet 4.5
- Claude Opus 4.5
- Claude Haiku 4.5

### Q: 如何自定义管理页面路径？

**A**: 设置环境变量：

```bash
ADMIN_PATH=/my-secret-admin node src/index-new.js
```

或在数据库中修改：

```sql
UPDATE system_settings SET value='/my-secret-admin' WHERE key='admin_path';
```

### Q: 数据库在哪里？

**A**: 默认位置：`./data/database.db`

建议定期备份数据库文件。

### Q: 如何备份数据？

**A**:

```bash
# 备份数据库
cp ./data/database.db ./data/database.db.backup

# 或使用时间戳
cp ./data/database.db ./data/database.db.$(date +%Y%m%d_%H%M%S)
```

### Q: 服务器崩溃了怎么办？

**A**:

1. 查看日志：`tail -100 /tmp/server.log`
2. 检查端口占用：`lsof -i:9091`
3. 重启服务器：`PORT=9091 node src/index-new.js`

### Q: 如何限制用户的请求频率？

**A**: 目前系统通过余额控制使用量。如需添加频率限制，请联系开发者。

---

## 🔒 安全建议

1. **修改默认管理密钥**：首次部署后立即修改
2. **使用 HTTPS**：生产环境建议配置 SSL 证书
3. **定期备份数据库**：避免数据丢失
4. **监控异常请求**：定期查看请求日志
5. **限制管理后台访问**：使用防火墙或反向代理限制访问 IP

---

## 📞 技术支持

如有问题，请检查：
1. 服务器日志：`/tmp/server.log`
2. 数据库文件：`./data/database.db`
3. 浏览器控制台错误信息

---

## 📝 更新日志

### v2.0.0 (2026-02-08)

- ✅ 多用户系统
- ✅ 计费管理
- ✅ SQLite 数据库
- ✅ 用户/管理员双界面
- ✅ Kiro 账号池管理
- ✅ 实时统计
- ✅ 品牌更新为 ClaudeAPI

---

## 📄 许可证

本项目基于原 Kiro2api-Node 项目改造而成。

---

**🎉 享受使用 ClaudeAPI！**
