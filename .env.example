# 服务器配置
PORT=8080
NODE_ENV=production

# API 密钥配置
API_KEY=sk-your-api-key-here
ADMIN_KEY=your-admin-key-here

# 数据存储
DATA_DIR=./data

# AWS 配置
REGION=us-east-1
KIRO_VERSION=0.8.0

# 对话历史限制（可选，不配置则不限制）
# 例如：MAX_HISTORY_TURNS=40 表示最多保留 40 轮对话（80 条消息）
# MAX_HISTORY_TURNS=40

# 工具结果大小限制（可选，默认 50000 字符）
# 如果工具返回的内容过长，会自动截断以避免请求过大
# MAX_TOOL_RESULT_LENGTH=50000

# 代理配置（可选）
# PROXY_URL=http://127.0.0.1:7890
